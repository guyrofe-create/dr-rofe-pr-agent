name: Legacy Manual Reviewed Draft Generator

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Generate a manually requested review draft
        run: python scripts/daily_run.py
        env:
          RUN_MODE: generate
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Commit draft or publication record
        if: always()
        run: |
          git config user.name "dr-rofe-pr-agent"
          git config user.email "actions@github.com"
          git add content_drafts/ || true
          git diff --cached --quiet || git commit -m "Update reviewed content workflow ${{ github.run_id }}"
          git pull --rebase || true
          git push || true

      - name: Upload run log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: run-log-${{ github.run_id }}
          path: |
            run_log.txt
          retention-days: 30
